'use client';
import Link from 'next/link';
import type { Scenario } from '@/lib/types';
const d={Easy:'bg-emerald-500/10 text-emerald-400 border-emerald-500/20',Medium:'bg-amber-500/10 text-amber-400 border-amber-500/20',Hard:'bg-red-500/10 text-red-400 border-red-500/20'};
export function ScenarioCard({scenario,callCount,avgScore}:{scenario:Scenario,callCount?:number,avgScore?:number}){return(<div className='group relative bg-surface border border-border rounded-2xl p-5 hover:border-accent/40 transition-all duration-200'><div className='flex items-start justify-between mb-3'><span className={`text-xs font-medium px-2.5 py-1 rounded-full border ${d[scenario.difficulty]}`}>{scenario.difficulty}</span>{avgScore!=null&&<span className='text-xs text-muted font-mono'>{Math.round(avgScore)} avg</span>}</div><h3 className='text-base font-semibold text-primary mb-1 group-hover:text-accent transition-colors'>{scenario.title}</h3><p className='text-sm text-muted leading-relaxed mb-3'>{scenario.summary}</p><div className='flex items-center gap-2 mb-4'><div className='w-1.5 h-1.5 rounded-full bg-accent/60'/><p className='text-xs text-muted italic'>{scenario.win_condition}</p></div><div className='flex items-center gap-2'><Link href={`/training?scenario=${scenario.id}&mode=text`} className='flex-1 text-center py-2 px-3 bg-accent text-white text-sm font-medium rounded-xl hover:bg-accent/90 transition-colors'>Text call</Link><Link href={`/training?scenario=${scenario.id}&mode=voice`} className='flex-1 text-center py-2 px-3 bg-surface border border-border text-sm font-medium rounded-xl hover:border-accent/40 hover:text-accent transition-all'>Voice call</Link></div>{callCount!=null&&<p className='mt-3 text-xs text-muted text-center'>{callCount} calls completed</p>}</div>);}
